# Trigger

This skill is packaged in a top-level folder named `fable-prompt` and its `SKILL.md` metadata uses `name: fable-prompt`.

Invoke it with:

```text
/fable-prompt
```

You can also pass a task directly:

```text
/fable-prompt Build a dashboard for my onboarding metrics and discover unknowns first
```
